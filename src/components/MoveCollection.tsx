import { useQuery } from "@apollo/client";
import {
  GetFuzzyLearnsetQuery,
  GetFuzzyLearnsetQueryVariables,
} from "../GraphQL/codegen-types";
import { GET_MOVES } from "../GraphQL/Queries/MoveQueries";

const MoveCollection = (pokemon: string) => {};
